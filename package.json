{
  "name": "slimey-web-component",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && vite build --config vite.component.config.js",
    "test": "node --test tests/*.test.js",
    "preview": "vite preview --host 127.0.0.1",
    "format": "prettier --write src tests index.html *.js *.json README.md scripts",
    "format:check": "prettier --check src tests index.html *.js *.json README.md scripts",
    "predeploy": "npm run build",
    "deploy": "node scripts/deploy-pages.js"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "vite": "^8.2.2"
  },
  "description": "Interactive viscous slime and fluid text for the web, rendered with Three.js.",
  "license": "MIT"
}
